diff -ruN at-spi-1.30.1.orig/Makefile.am at-spi-1.30.1/Makefile.am
--- at-spi-1.30.1.orig/Makefile.am	2009-11-09 07:14:28.000000000 +0000
+++ at-spi-1.30.1/Makefile.am	2012-05-15 22:48:53.373477773 +0100
@@ -12,11 +12,11 @@
 
 if GCONF_SCHEMAS_INSTALL
 install-data-local:
-                if test -z "$(DESTDIR)" ; then \
-                        for p in $(schemas_DATA) ; do \
-                                GCONF_CONFIG_SOURCE=$(GCONF_SCHEMA_CONFIG_SOURCE) $(GCONFTOOL) --makefile-install-rule $(top_builddir)/data/$$p ; \
-                        done \
-                fi
+	if test -z "$(DESTDIR)" ; then \
+	  for p in $(schemas_DATA) ; do \
+	    CONF_CONFIG_SOURCE=$(GCONF_SCHEMA_CONFIG_SOURCE) $(GCONFTOOL) --makefile-install-rule $(top_builddir)/data/$$p ; \
+	  done \
+	fi
 else
 install-data-local:
 endif
